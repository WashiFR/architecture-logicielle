{% extends 'SkeletonView.twig' %}

{% block content %}
    {% for categorie in categories %}
        <h1>Catégories {{ categorie.id }} : {{ categorie.libelle }}</h1>
        <p>{{ categorie.description }}</p>
        <h2>Prestations</h2>
        <ul>
            {% for prestation in categorie.prestations %}
                <li><a href="{{ prestation.url }}">{{ prestation.libelle }}</a></li>
            {% endfor %}
        </ul>
    {% endfor %}
{% endblock %}